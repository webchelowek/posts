import React from 'react';
import "./MyButton.css";

const MyButton = (props) => {
    return (
        <button {...props} className="MyButton">
            
        </button>
    );
};

export default MyButton;